import React from 'react';

function Button() {
    return (
      <div className="button">
          <button  className="btn btn--block card__btn">Leia mais</button>
      </div>
  );
  // onClick={() => this.pegaDesenho(title)}

}

export default Button;